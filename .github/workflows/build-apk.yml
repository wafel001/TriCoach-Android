name: Build TriCoach APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
      - name: Install Android SDK 36
        run: yes | sdkmanager "platforms;android-36" "build-tools;35.0.0" || true
      - name: Set up Gradle 8.13
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'
      - name: Rebuild project archive
        run: |
          cat payload/project.b64.part* | base64 --decode > project-fixed.zip
          unzip -t project-fixed.zip
          rm -rf buildsrc
          mkdir buildsrc
          unzip -q project-fixed.zip -d buildsrc
          test -f buildsrc/TriCoach_Android/settings.gradle
      - name: Build debug APK
        working-directory: buildsrc/TriCoach_Android
        run: gradle --no-daemon :app:assembleDebug --stacktrace
      - name: Rename APK
        run: cp buildsrc/TriCoach_Android/app/build/outputs/apk/debug/app-debug.apk TriCoach-v1.0-debug.apk
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: TriCoach-Android-APK
          path: TriCoach-v1.0-debug.apk
          if-no-files-found: error
